# awclip-Testdateien

Kopie von `Assets/Animation Workbench/Scripts/Editor/Community/Format/Fixtures~`
aus dem Animation-Workbench-Repo. Dort liegen auch `make_fixtures.py` (erzeugt
die Dateien und rechnet die Hashes unabhängig in Python nach) und die
C#-Tests, die dieselben Erwartungen prüfen.

Ändert sich das Format, ändern sich beide Kopien im selben Zug - sonst prüfen
Client und Server unterschiedliche Dinge.
